# ── Supabase ───────────────────────────────────────────────────────────────
# Project URL from: Supabase dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=

# Public anon key (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — server only, never expose to the browser
SUPABASE_SERVICE_ROLE_KEY=

# ── Web Push (VAPID) ───────────────────────────────────────────────────────
# Generate once with:  npx web-push generate-vapid-keys
#
# NEXT_PUBLIC_VAPID_PUBLIC_KEY  — sent to the browser (must match VAPID_PUBLIC_KEY)
# VAPID_PUBLIC_KEY              — server-side copy of the same public key
# VAPID_PRIVATE_KEY             — keep secret, server only
# VAPID_SUBJECT                 — contact URI shown in push requests (mailto: or https:)
#
# Leave all four empty to disable Web Push (the app works without them).

NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

# ── Cron ───────────────────────────────────────────────────────────────────
# Secret token for /api/cron/* routes.
# Set the same value in your cron job Authorization header:
#   Authorization: Bearer <CRON_SECRET>
CRON_SECRET=
